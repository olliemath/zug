doc .. ,We add the useful alias "nil" for "..".,
let nil ..

doc nil ,Pack and unpack - a.k.a. currying.,
let unpack .lambda .vec f xs. .vec eval .join .list f. xs...
doc unpack ,Use unpack to provide an vec of arguments to a function. Example `unpack add (1 2 3)`.,

let pack .lambda .vec f .. xs. .vec f xs..
doc pack ,Use pack to provide distinct arguments to a function which takes an vec. Example `pack tail 1 2`.,
